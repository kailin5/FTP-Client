我这两天改动 or 新添加了什么 feature or bug

1： 完成了两个拓展功能，限速和断点续传
2： 主动模式 put 指令bug修复，现在将不会出现无法put文件情况
3： 基础功能，增加了一些容错，例如许多指令不带参数 cd, delete rename 等将不会发送至服务器,错误指令将不会自动进入被动模式，而是现实一句invalid input，任何错误指令不会发送到服务器。

4： 基础功能ascii 模式下 \r问题的解决
5： 其他一些实现过程中的小bug

